// Test generated by RoostGPT for test nodeJsTest1 using AI Type Azure Open AI and AI Model roost-gpt4-32k

import { getUrlPath } from "./postmanCollectionProcessor";

describe("getUrlPath method", () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test("should correctly form path when urlObj contains both path and query params", () => {
    const mockUrlObj = {
      path: ["v1", "users"],
      query: [{ key: "id", value: "123" }]
    };

    const result = getUrlPath(mockUrlObj);
    expect(result).toBe("/v1/users?id=123");
  });

  test("should correctly form path when urlObj contains only path", () => {
    const mockUrlObj = {
      path: ["v1", "users"]
    };

    const result = getUrlPath(mockUrlObj);
    expect(result).toBe("/v1/users");
  });

  test("should correctly form path when urlObj contains only query params", () => {
    const mockUrlObj = {
      query: [{ key: "id", value: "123" }]
    };

    const result = getUrlPath(mockUrlObj);
    expect(result).toBe("?id=123");
  });

  test("should return an empty path for an empty urlObj", () => {
    const mockUrlObj = {};

    const result = getUrlPath(mockUrlObj);
    expect(result).toBe("");
  });
});
