// Test generated by RoostGPT for test nodeJsTest1 using AI Type Azure Open AI and AI Model roost-gpt4-32k

import { generateTestCase } from "../postmanCollectionProcessor.js"
const responseObj = {
  code: 200,
  name: "Successful operation",
  status: "OK",
  body: '{}',
  header: [{ key: "Content-Type", value: "application/json" }]
};
const methodType = "GET";
const swaggerObj = {
  responses: {
    "200": {
      description: "successful operation",
      content: {
        "application/json": {
          schema: {
            properties: {
              id: { type: "number", maximum: 100, minimum: 1 },
              name: { type: "string" }
            }
          }
        }
      }
    }
  }
};
let testScript = "";

describe('test generateTestCase function', () => {
  test('generates test script for a valid response', async () => {
    const result = await generateTestCase(testScript, responseObj, methodType, swaggerObj, swaggerContent);
    expect(result).toMatchSnapshot();
  });

  test('throws error for empty response object', async () => {
    await expect(generateTestCase(testScript, {}, methodType, swaggerObj, swaggerContent)).rejects.toThrow();
  });
});
