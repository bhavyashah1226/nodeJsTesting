// Test generated by RoostGPT for test nodeJsTest1 using AI Type Azure Open AI and AI Model roost-gpt4-32k

import { addVariable } from './postmanCollectionProcessor.js';


describe('addVariable Method in postmanCollectionProcessor.js', () => {
  let variablesSet;

  beforeEach(() => {
    variablesSet = new Set();
  });

  test('should add variable to the set', () => {
    addVariable('testVariable');
    expect(variablesSet.has('testVariable')).toBe(true);
  });

  test('should handle when variable is null', () => {
    addVariable(null);
    expect(variablesSet.size).toBe(0);
  });

  test('should handle when variable is undefined', () => {
    addVariable(undefined);
    expect(variablesSet.size).toBe(0);
  });

  test('should not add duplicate variables', () => {
    addVariable('testVariable');
    addVariable('testVariable');
    expect(variablesSet.size).toBe(1);
  });
});
